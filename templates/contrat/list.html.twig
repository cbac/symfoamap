{% extends 'base.html.twig' %}

{% block body %}
    <h1> {{ titre }}</h1>
<div class="col-md-6">
 <p>
 {{ form_start(new_form) }}
   {#} <input type="submit" value="New"> #}
    <button class="btn btn-lg btn-primary">Nouveau {{ titre }}</button>
    </p>
{{ form_end(new_form) }}
 <table class="table table-bordered" >
    <tbody>
      {% for index, contrat in contrats %}
        <tr>
          <th align="center">{{ contrat.personne }}</th>
          <td>{{ form_start(showforms[index]) }}
                <button class="btn btn-lg btn-success">Show</button>
               {{ form_end(showforms[index]) }}
          </td>
          <td>{{ form_start(editforms[index]) }}
                 {# } <input type="hidden" value="Edit"> #}
                  <button class="btn btn-lg btn-warning">Edit</button>
                  {{ form_end(editforms[index]) }}
          </td>
           <td> {{ form_start(deleteforms[index]) }}
                 {#}<input type="hidden" value="Delete"> #}
                  <button class="btn btn-lg btn-danger">Delete</button> 
                  {{ form_end(deleteforms[index]) }}
           </td>
         </tr>
         <tr>
         <td colspan="4">
           <table class="table-bordered table table-condensed" >
       <thead>
        <tr>
            <th>Produit</th>
            <th align="right">Nombre</th>
        </tr>
    </thead>    <tbody>
        {% for ligne in contrat.lignes %}
        <tr>
            <td> {{ ligne.produit }} </td>
            <td align="right"> {{ ligne.nombre }} </td>
        </tr>
        {% endfor %}
     </tbody>
    </table>
         </td>
         </tr>
      {% endfor %}
    </tbody>
  </table>
</div>
{% endblock %} {# body #}
