<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}SF Amap Farine{% endblock %}</title>
{% block stylesheets %}
<!-- 		   <link rel="stylesheet" href="{{ asset('build/css/app.css') }}" />
-->
<link
	href="https://maxcdn.bootstrapcdn.com/bootswatch/3.2.0/darkly/bootstrap.
min.css"
	rel="stylesheet">
<link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css">
<link rel="stylesheet" type="text/css" media="print" href="/css/bootstrap.min.css">

{% endblock %}
   </head>

{% block navbar %} 
    <nav class="navbar navbar-inverse">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{ path('homepage') }}">Amap farine</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
              <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hors contrats <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="{{ path('horscontrat_list') }}">Liste des commandes hors contrats</a></li>
				<li><a href="{{ path('horscontrat_byperson') }}">Liste des commandes hors contrat pour la distribution</a></li>
				<li><a href="{{ path('horscontrat_byproduit') }}">Liste des produits en commande hors contrat</a></li>
                <li><a href="{{ path('horscontrat_new') }}">Nouvelle commande hors contrat</a><li>
              </ul>
            </li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Contrats <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="{{ path('contrat_list') }}">Liste des contrats</a></li>
				<li><a href="{{ path('contrat_byperson') }}">Liste des contrats pour la distribution</a></li>
				<li><a href="{{ path('contrat_byproduit') }}">Liste des produits en contrat</a></li>
                <li><a href="{{ path('contrat_new') }}">Nouveau contrat</a><li>
              </ul>
            </li>
    <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Adhérents <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="{{ path('personne_index') }}">Liste des adhérents</a></li>
                <li><a href="{{ path('personne_new') }}">Nouvel adhérent</a><li>
              </ul>
            </li>
    <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Produits <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><a href="{{ path('produit_index') }}">Liste des produits</a></li>
                <li><a href="{{ path('produit_new') }}">Nouveau produit</a><li>
              </ul>
            </li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
{% endblock %}

{%  block alerts %}
 {% for type, messages in app.session.flashBag.all %}
    {% for message in messages %}
        {%if type == 'error'%} {% set type = 'danger' %} {%endif%}
        {%if type == 'message'%} {% set type = 'info' %} {%endif%}
        <div class="alert alert-{{ type }} alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert">
                <span aria-hidden="true">&times;</span>
                <span class="sr-only">Close</span>
            </button>
            <p>{{ message|raw }}</p> 
        </div>
    {% endfor %}
{% endfor %}
{% endblock %} {# alerts #}
 <body>
{% block body %}
   <div style="min-height: 90%" class="container">   
       {% block menu %}
        {{ block('navbar') }}
        {% endblock %} {# menu #}
       {{ block('alerts') }}
       <div class="container body-container"> 
            {% block main %}
            <div class="row">
                <div class="col-md-12">
                    <p><i>MAIN</i></p>
                </div>
            </div>
            {% endblock %} {# main #}
        </div>  <!-- /.body-container -->
        {% block footer %}
         <div class="row">
            <div class="col-md-12">
                <footer>
                    <p>FOOTER</p>
                </footer>
            </div>
        </div>
        {% endblock %} {# footer #}

    </div>  <!-- /.container -->
{% endblock %} {# body #}
</body>
{% block javascripts %}
 	   <script src={{ asset('build/js/app.js') }}></script>
{% endblock %}
{% block custompage_script %}
{% endblock %}
 
</html>

